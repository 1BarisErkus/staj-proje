import { DescriptionText } from "@/styles/ProductDetail/Features";

const Description = ({ description }: { description: string }) => {
  return <DescriptionText>{description}</DescriptionText>;
};

export default Description;
